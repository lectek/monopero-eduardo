# Build output
target/
*.class

# Local Maven cache used during this setup (not project config)
.m2tmp/

# IDE
.idea/
.vscode/
*.iml

# Real inventory database (contains business data) — never commit
ims_files/rbp.db
ims_files/rbp.dp

# Unused legacy artifacts — not required by the app (sqlite-jdbc bundles its
# own native library at runtime); kept locally only if you use them manually
# with a SQLite browser/CLI to edit the schema
ims_files/sqlite3.dll
ims_files/sqlite3.def

# OS cruft
.DS_Store
Thumbs.db
